interface:
  display_name: Principle Sequence Verifiable Units
  short_description: Apply to multi-step work (sweeps, migrations, runs of similar
    edits) and to how you stack commits and PRs
  default_prompt: Use $principle-sequence-verifiable-units for this task.
policy:
  allow_implicit_invocation: false
