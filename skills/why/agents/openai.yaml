interface:
  display_name: Why
  short_description: Use for 'why does X work this way', 'why we picked Y', design
    rationale, regressions, postmortems, or data-backed thresholds
  default_prompt: Use $why for this task.
policy:
  allow_implicit_invocation: false
