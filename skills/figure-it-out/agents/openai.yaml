interface:
  display_name: Figure It Out
  short_description: 'Design an auditable playbook when no narrower one fits: a large
    migration, an ambitious multi-part change, or work a human reviews after stepping
    away'
  default_prompt: Use $figure-it-out for this task.
policy:
  allow_implicit_invocation: false
