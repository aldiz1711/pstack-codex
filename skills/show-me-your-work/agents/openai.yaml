interface:
  display_name: Show Me Your Work
  short_description: 'Keep a reviewable decision trail for long-running or unattended
    work: a TSV log with one row per decision (what, why, evidence, result)'
  default_prompt: Use $show-me-your-work for this task.
policy:
  allow_implicit_invocation: false
