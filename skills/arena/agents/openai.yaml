interface:
  display_name: Arena
  short_description: Spawn N parallel candidates at the same task, pick a base, graft
    the strongest parts of the losers into it
  default_prompt: Use $arena for this task.
policy:
  allow_implicit_invocation: false
